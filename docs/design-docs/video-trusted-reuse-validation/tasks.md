# Video 可信复用验证任务

> 来源：本目录 `spec.md`
> 状态：completed；PASS by explicit user override
> 冻结样本：仅 V01–V03 最终 reading
> 实现约束：观察期间不改产品代码、不调用模型、不增加样本

## Task 0：[x] 冻结规范与材料

- [x] 用户显式授权进入最小 Video 可信复用验证；
- [x] 绑定 V01–V03 最终 reading 的路径、Bytes、SHA-256、Bundle ID 和 Draft ID；
- [x] 冻结 PASS / NO-SIGNAL / FAIL Gate；
- [x] 创建只追加、不回写历史的观察日志；
- [x] 明确本阶段不启动 PDF、X0-B、C0、Review/Publisher、Engine、Vault 或 Desktop。

## Task 1：[x] 建立 Day 0 基线

- [x] 三份 reading 在本地存在并与冻结 Bytes、SHA-256 匹配；
- [x] 沿用 Pilot 最终用户判定 V01/V02/V03 PASS，作为“愿意保留”的 Day 0 弱证据；
- [x] 记录开始日期 2026-07-30；最早结束日期 2026-08-06；
- [x] 前 72 小时不做计划性检索。

## Task 2：[x] 记录用户整体结论

原计划是在 7 天窗口中逐项记录自然复用；用户于 2026-07-30 提前结束观察并声明整体通过。用户未逐项提供自然复用次数，因此记录为产品 Owner override，不编造事件明细。

- 触发任务；
- 使用样本；
- 是否避免重看视频；
- 是否支持了决策、实现、解释或比较；
- 必要人工修订分钟数；
- 发现的问题。

重复评分、为了完成本任务而打开文件、固定延迟检索都不算自然复用。

## Task 3：[x] 处理未执行的延迟检索

冻结问题和原 Gate 继续保留，但用户在计划执行日期前显式判定整体 PASS。三题耗时没有独立记录；本任务以 override 关闭，不把未执行检查写成 `3/3`。

- 开始/定位时间；
- 是否在 3 分钟内定位；
- 是否重看视频；
- 对应标题或段落；
- 答案是否足以支持问题。

不得临时更换为更容易的问题。

## Task 4：[x] 处理未到期的 Day 7 复核

- [x] Day 0 的三份 reading SHA-256 已验证；
- [x] 用户显式声明整体通过，并说明格式优化可在后续使用中渐进处理；
- [x] 原定 Day 7 数据未到期，不伪造为已执行；
- [x] 以产品 Owner override 结束观察。

## Task 5：[x] 冻结结论

只能选择一个：

- `PASS`：完整性与保留 3/3、延迟检索 3/3、自然复用至少 1 次、清洗不超过 10 分钟；
- `NO-SIGNAL`：延迟检索 3/3，但没有自然复用；
- `FAIL`：命中 `spec.md` 的任一失败条件。

最终结论为 `PASS by explicit user override`。结论写入观察日志和总任务清单，不回写 `VAL-VIDEO-001`。用户已同时授权继续后续开发，因此下一步允许冻结一个真实 born-digital PDF 并启动最小 X0-B；不得以 Fake PDF/Recipe 代替真实输入。
