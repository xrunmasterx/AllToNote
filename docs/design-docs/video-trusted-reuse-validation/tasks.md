# Video 可信复用验证任务

> 来源：本目录 `spec.md`
> 状态：observation ready；7 天窗口进行中
> 冻结样本：仅 V01–V03 最终 reading
> 实现约束：观察期间不改产品代码、不调用模型、不增加样本

## Task 0：[x] 冻结规范与材料

- [x] 用户显式授权进入最小 Video 可信复用验证；
- [x] 绑定 V01–V03 最终 reading 的路径、Bytes、SHA-256、Bundle ID 和 Draft ID；
- [x] 冻结 PASS / NO-SIGNAL / FAIL Gate；
- [x] 创建只追加、不回写历史的观察日志；
- [x] 明确本阶段不启动 PDF、X0-B、C0、Review/Publisher、Engine、Vault 或 Desktop。

## Task 1：[x] 建立 Day 0 基线

- [x] 三份 reading 在本地存在并与冻结 Bytes、SHA-256 匹配；
- [x] 沿用 Pilot 最终用户判定 V01/V02/V03 PASS，作为“愿意保留”的 Day 0 弱证据；
- [x] 记录开始日期 2026-07-30；最早结束日期 2026-08-06；
- [x] 前 72 小时不做计划性检索。

## Task 2：[ ] 记录自然复用

在 7 天窗口中，只要用户因为真实任务自然返回任一笔记，就向 `observation-log.md` 追加一行：

- 触发任务；
- 使用样本；
- 是否避免重看视频；
- 是否支持了决策、实现、解释或比较；
- 必要人工修订分钟数；
- 发现的问题。

重复评分、为了完成本任务而打开文件、固定延迟检索都不算自然复用。

## Task 3：[ ] 完成第 4–7 天延迟检索

按 `spec.md` 中冻结的三个问题逐一执行。每题记录：

- 开始/定位时间；
- 是否在 3 分钟内定位；
- 是否重看视频；
- 对应标题或段落；
- 答案是否足以支持问题。

不得临时更换为更容易的问题。

## Task 4：[ ] 第 7 天完整性与保留复核

- [ ] 重新计算三份 reading 的 SHA-256；
- [ ] 确认三份材料仍可打开；
- [ ] 对 V01、V02、V03 分别记录“继续保留 / 删除”；
- [ ] 汇总最长人工清洗时间与阻塞性事实错误。

## Task 5：[ ] 冻结结论

只能选择一个：

- `PASS`：完整性与保留 3/3、延迟检索 3/3、自然复用至少 1 次、清洗不超过 10 分钟；
- `NO-SIGNAL`：延迟检索 3/3，但没有自然复用；
- `FAIL`：命中 `spec.md` 的任一失败条件。

结论写入观察日志和总任务清单，不回写 `VAL-VIDEO-001`。只有 PASS 才允许请求进入一个 born-digital PDF + 最小 X0-B；是否实际启动仍需用户显式确认。
