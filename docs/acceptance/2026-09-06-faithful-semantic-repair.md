# 保真图文语义修复闭环复核（2026-09-06）

结论：已修复“模型复核失败直接终止，无法反馈编辑”的流程。指定 B 站视频成功生成并通过自动复核；人工式内容检查仍发现 ASR 错词残留，不能称为免校对成品。

## 实现

- `backend/app/core/recipes/video/faithful_edition/review.py`：合法的不通过结果保留正文、摘要问题，不在解析器中提前终止；不一致布尔值、未知帧、重复帧等格式问题仍拒绝。
- `backend/app/core/application/faithful_edition_compiler.py`：最多一轮语义修复，只重写失败章节。请求包含原转录、旧稿、正文/摘要复核意见和同段真实 WebP；修复后先重新执行确定性校验，再复核失败章节。
- 初次复核与再次复核使用不同分片键，仍经持久化 ModelCallCoordinator；测试确认恢复不重放模型调用。最终图注采用再次复核结果，不沿用失败结果。
- 原有格式/数值修复最多一轮不变；新增语义修复最多一轮，所以最大串行阶段数由 3 改为 5，修复调用数最多为章节数的两倍。正文再次失败仍停止，摘要再次失败则省略。
- `backend/app/runtime.py` 增加冻结行为标识 `semantic_repair: 1`；`video_service.py` 同步验证上限。未改模型、鉴权、传输或 CLI 默认输出模式。
- 提示输入分离参考 [OpenAI Prompt engineering](https://developers.openai.com/api/docs/guides/prompt-engineering)，将复核意见作为待验证建议而非权威，原文与图片仍为证据。

## 验证

- 18 个相关模块：499 项通过，105.56 秒。
- 随后补充“只修复/重查失败章节”测试；保真编译模块 56 项通过，6.57 秒（包含该新增测试及前述集合中的 55 项）。
- 覆盖正文失败、仅摘要失败、格式修复后再语义修复、真实图片输入、修复后数字变化被拒绝、再次失败停止、持久化恢复和旧图注丢弃。
- `git diff --check` 通过。
- 独立本地 Chromium 实测：3 个折叠块全部默认展开，收起/展开正常，紧随所属章节标题且位于正文之前；8 张图片全部加载；中文无乱码，无可见“待核对”。

## 真实运行

- 输入沿用 `local-data/inputs/BV1ntt66WEzy/BV1ntt66WEzy.mp4`，不重新下载。
- Job：`job_01a076fa-8b5a-713b-a4f4-d8a738bcbd78`
- Run：`run_01a076fa-8b5a-778a-b3c8-bce7cb67477a`
- Bundle：`bnd_01a076fa-8b5a-7a7c-b578-d88ac4f41e7b`
- Manifest SHA256：`b5ffcdbee027b1753a4060adeb7eacf0a6ce5364718481a5ff814c41790bd333`
- Commit SHA256：`92deb3ad80041d76987c61a63647b0930879e56a2f2dc72d32a53ab28dbed240`
- 自动质量：`pass`，允许发布。3 章、8 张配图、10 次模型调用、2 次语义修复、4 个串行阶段，正文来源引用覆盖率 1.0。
- 实际触发修复的章节为第 2、3 章：复核要求不要无证据将“即刻竞价”改成“集合竞价”；另一处“堆移了”先被改成“堆积了”，修复结合图片字幕改为“对应的”。修复后的正文与摘要再次复核均通过。
- 输出：`local-data/outputs/bilibili-faithful-BV1ntt66WEzy-20260906-reading.{md,html}`；审计 Bundle 副本在同名前缀目录中，未改写原 Bundle。

## 人工式检查边界

通读导出的正文和摘要，逐张查看 8 张图片。图注均对应所示图表/字幕；第 3 章“对应的0.88”可在实际截图字幕确认。
仍有明显转录错词，例如“看见久转”与画面“看跌9转”不符，图注识别了正确术语，但正文/摘要没有自动更正。
其他如“超级”“BGC”等疑词仍被保守保留。当前确定性数值规则及“允许原样保留含糊 ASR”的复核规则，使自动通过不等价于转录纠错完整。
本次没有手改输出伪造模型修复效果，没有重建 V20，也没有完整音频复听或跨视频泛化验收。
